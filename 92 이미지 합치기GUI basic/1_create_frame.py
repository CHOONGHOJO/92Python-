from tkinter import *

root = Tk()
root.title("나도코딩 GUI")
# root.geometry("640x480+300+200") # 가로 * 세로 + x 좌표 + y 좌표
root.geometry("640x480") # 가로 * 세로
root.resizable(False, False) # x(너비), y(높이) 값 변경 불가 (창크기 변경 불가)

root.mainloop()
